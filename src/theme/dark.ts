import { palette } from "../constants/colors";

export const darkTheme = {
  accent: palette.aqua,
  background: palette.ink,
  border: "rgba(255,255,255,0.1)",
  card: palette.panel,
  danger: palette.coral,
  muted: palette.mist,
  primaryText: palette.sand,
  secondaryText: "#A5B7C7",
} as const;
