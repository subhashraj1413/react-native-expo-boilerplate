export default {
  accountBody: "Account controls live inside the shared protected drawer.",
  accountSignedInAs: "Signed in as {{email}}",
  accountTitle: "Account",
  body:
    "Open the drawer from any protected screen to move between workspace, settings, and account without leaving the signed-in shell.",
  openAccount: "Open account section",
  profileModal: "Profile modal",
  signOutBody: "Signing out clears the session state and resets RTK Query caches via saga.",
  signOutTitle: "Sign out",
  title: "Settings now lives inside the protected drawer.",
} as const;
